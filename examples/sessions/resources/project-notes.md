# Project notes

The prototype should expose only completed turns and resources explicitly selected by the owner during projection.

The first client integration will use MCP so that a GPT client can discover Prism tools.
